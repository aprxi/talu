"""Tests for talu.profile module.

Maps to: talu/profile.py

Named ``talu_profile`` (not ``profile``) to avoid shadowing the stdlib
``profile`` module, which is imported internally by ``cProfile`` / PyTorch.
"""
